# Rule: Auto Git Update

Always commit and push changes to the remote Git repository (`origin main`) at the end of every iteration/work step.

1. Stage modified files (`git add .`)
2. Commit with a descriptive message (`git commit -m "..."`)
3. Push to remote (`git push origin main`)
